<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	package="mediaaction.android">

	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_mediaactions_logo"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_mediaactions_logo_round"
		android:supportsRtl="true"
		android:theme="@style/AppTheme"
		android:usesCleartextTraffic="true"
		tools:targetApi="m">
		<activity android:name=".ui.ConnectionActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<activity android:name=".ui.RegisterActivity" />
		<activity android:name=".ui.ForgetPasswordActivity" />
		<activity android:name=".ui.RequestWallFragment" />
		<activity android:name=".ui.RequestActivity" />
		<activity android:name=".ui.PhotoDetailActivity" />
		<activity
			android:name=".ui.HomeActivity"
			android:label="@string/title_activity_home" />

		<provider
			android:name="android.support.v4.content.FileProvider"
			android:authorities="${applicationId}.com.vansuita.pickimage.provider"
			android:exported="false"
			android:grantUriPermissions="true"
			tools:replace="android:authorities">
			<meta-data
				android:name="android.support.FILE_PROVIDER_PATHS"
				android:resource="@xml/picker_provider_paths" />
		</provider>

		<activity android:name=".ui.ParametersFragment"></activity>
	</application>

</manifest>